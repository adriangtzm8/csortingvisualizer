#ifndef VISUALIZER_H
#define VISUALIZER_H

#include <stdio.h>
#include "raylib.h"

void visualizeArray(int* arr, int len);
int findMax(int* arr, int len);
int calculateHeight(int val, int max);

#endif // VISUALIZER_H
